syntax = "proto3";

package chat;

message ChatMessage {
  string user = 1;
  string text = 2;
  string response = 3;
}

service ChatService {
  rpc Chat(stream ChatMessage) returns (stream ChatMessage);
}